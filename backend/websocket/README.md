# WebSocket service

Real-time cross-device channel: clipboard sync, live annotations, gesture events, presence.

Can run as a separate Railway service or mount on FastAPI via `websockets` / Starlette.
