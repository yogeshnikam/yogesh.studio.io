# Infrastructure

Local and production-adjacent config:

- `docker/` — compose files, Dockerfiles
- `mongodb/` — init scripts, indexes
- `redis/` — redis.conf or cache patterns
- `livekit/` — livekit.yaml for self-hosted dev

Run full stack locally once `docker-compose.yml` is added in Phase 2+.
