# Shared packages

| Package | Purpose |
|---------|---------|
| `shared-types` | TypeScript types + JSON schemas shared by web and mobile |
| `shared-utils` | API client helpers, room IDs, validation |
| `config` | ESLint, TS config, env schema (optional) |
